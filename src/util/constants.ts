/**
 * Logger function, I just wanted to place something I'm using on more places into one signe location,
 * since we need to have this on every component to track how many times they are rendered.
 */
export function logger(message: string) {
  console.log(message);
}
